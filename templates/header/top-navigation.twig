{% import "/templates/macros/common/mdg-navigation/mdg-navigation.twig" as topNavigation %}

{% set menuConfig = {
	"menuId": top_menu,
	"type":"top",
    "allowChildren": false
} %}

{{ topNavigation.mdg_navigation(menuConfig) }}

